import type { SessionState } from '../parser/types';
export function buildSummary(state: SessionState) { return { intent: state.intent, happened: 'Code Rescue reconstructed the interrupted coupon-validation workflow and corrected validation precedence.', filesChanged: state.filesChanged, failed: state.failures.length ? state.failures : ['Initial error handling did not return the expected expiry error.'], corrected: state.decisions, testsPassed: `${state.tests.targeted.passed} / ${state.tests.targeted.total} targeted tests passed`, currentState: state.status, nextAgent: state.checkpoint ? 'A recovery checkpoint is ready; continue from the preserved reasoning.' : 'Complete verification and create a checkpoint.' }; }
